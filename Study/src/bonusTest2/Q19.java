package bonusTest2;

public class Q19 {
    //thrown programmatically(вручную) ALWAYS:
    /*
    java.io.IOException() is thrown by many methods that read/write files and is always thrown programmatically (Always thrown programmatically — either by your code or library code)
    NumberFormatException is thrown when converting numbers.
     */
}
